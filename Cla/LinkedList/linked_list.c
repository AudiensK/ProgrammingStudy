#include <stdio.h>
#include "node.h"


// typedef struct _node {
//     int value;
//     struct _node *next_p;
// } Node;


int main(int argc, char const* argv[])
{
    int number;
    do {
        scanf("%d", &number);
        if (number != -1) {

        }
    } while (number != -1);

    return 0;
}